GARMIN_USERNAME=<your username / email>
GARMIN_PASSWORD=<your password>

# Withings OAuth2 tokens (for CI/CD - obtain from initial local run)
# After running withings-sync locally, find these in ~/.withings_user.json
# WITHINGS_ACCESS_TOKEN=<access_token from .withings_user.json>
# WITHINGS_REFRESH_TOKEN=<refresh_token from .withings_user.json>
# WITHINGS_USERID=<userid from .withings_user.json>
